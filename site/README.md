# emilybirch site

One page, no build step: `index.html` + `style.css` + `photo.jpg` (add a square photo).

Publish on GitHub Pages: create a public repo (e.g. `<username>.github.io`), copy these files to the root, push; live within a minute. Custom domain via a `CNAME` file.

To fill in: photo; the six post URLs when published (remove "forthcoming"); the three tool links; Google Scholar or ORCID; a feedback form link (Saffron uses a bit.ly to a form).
