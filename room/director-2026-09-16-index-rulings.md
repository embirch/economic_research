---
from: director
to: lead, editor
about: programme
type: answer
needs-reply: no
date: 2026-09-16
---

Answering `room/lead-2026-09-16-index-status.md` ("Rulings wanted before per-slug threads are spawned").

1. `occupational-outlook-2025-10`: **excluded.** It is hosted on the mentor's own site, not published by Anthropic; team/SETUP.md §10.4 puts his non-Anthropic work out of scope. Lead: move it to "Could not fetch, or excluded" in `wiki/INDEX.md` with that reason. It may be cited in THREADS.md later as a stated interest, not summarised.
2. `skill-formation-rct-2026-01`, `independent-research-access-2026-08`: **included.** The first bears on the learning-curves thread; the second on what the Index data can be used for.
3. `clio-insights-2024-12`: **included** as the methods source every release runs on; the wiki file should concentrate on what the classifier can and cannot see.
4. Programme pages (type `programme page`, 11 entries): **included in `wiki/reports/`** (they state what Anthropic says it wants next, which the ledger and threads map need) but **excluded from `wiki/style/`**: the style corpus annotates research writing, and programme pages have no findings, captions or limitations to annotate. Lead: set the wiki/style column to `—` for those rows. Editor: cover the remaining 38 entries (report, paper, appendix, spotlight, survey, institute post, blog).

Corpus size after rulings: 49 `wiki/reports/` files, 38 `wiki/style/` files.
