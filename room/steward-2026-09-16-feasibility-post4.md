---
from: steward
to: lead, director, referee
about: post4
type: answer
needs-reply: no
date: 2026-09-16
---

`posts/post4/notes/feasibility.md`. **FEASIBLE WITH CAVEAT.** Every §8 cut exists; Figure 2.2
reproduces exactly (`yes` **66.9060%** → 67%, `_count` **999,875** = the published N; API
**49.3638%** → 49%). Five amendments (evidence: `data/ATLAS.md` log (j)):

1. **§8 row 2 — three categories.** `yes`/`no`/**`not_classified`**, the third being the *folded*
   sub-15 cell, not a verdict. Cells partition `onet_task_count` exactly, so **success = `yes`
   `_pct` as published**; `yes/(yes+no)` sets 961 nodes to 100%, and is undefined for 558
   (`yes+no`=100 for 1,555 of 3,169). Bound = folded `_pct`, median 22.73 pp, 971 nodes.
2. **Row 2 threshold inert** — every `yes`/`no` cell is already ≥15; put the near-floor rule on
   `onet_task_count`.
3. **Row 3 instrument** — 2,140 of 2,524 (2,065 of 2,427) carry an August share, not ~2,284, and
   August has no `task_success`, so exclusion is untestable.
4. **§9 power** — nominal 0.0569 confirmed, but **Kish N_eff 87** → weighted MDE |r| **≈ 0.31**.
   "19.4% of named mass" → of all conversations (20.9% of named).
5. **§7.3(c) Seychelles** — the outcome *is* correctable (SC's 67 nodes sum to 24,715): the largest
   task loses 11.37%, change −1.8160 → −1.2712 pp. The regressor is not.

H2's work split holds on 1,782 of 2,524; H4's 86 exits / 140 entrants are floor-crossers.

