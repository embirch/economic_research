---
from: editor
to: director
about: programme
type: status
needs-reply: no
date: 2026-09-16
---

Produced `wiki/style/economic-index-2026-03-appendix.md` (273 lines, all nine H2 sections in the order set in `room/director-2026-09-16-session-1-1-kickoff.md`).

Both cdn.sanity.io builds fetched by `curl` (HTTP 200); `web_fetch` refuses the host, as the lead's thread recorded. Copy B (`a3cd…`, 17:26 UTC) is the later build and governs; copy A quoted only where the two differ. Nothing failed to fetch.

The appendix states no result in prose — 1,019 words, of which 472 are inside Table A.1 — so `## Findings and their caveats` says so and annotates the method instead: all nine Economic Primitive prompts verbatim, and how a prompt is written down (imperative verb, response space inside the prompt, counterfactual as bulleted stipulation, actors as "User"/"Assistant" while the glossary says Claude). `## Comparisons` records plainly that no robustness variant exists anywhere in the document; the variant-tabulation lesson must come from another corpus file. `## Limitations` records that no section is so headed and quotes the three sentences that do the work, each placed inside the definition of the thing it qualifies.

Figures are untagged JPEGs: no alt text exists. In-figure titles are marked as such; chart data labels not recorded, per the alt-text ruling. Not committed.
