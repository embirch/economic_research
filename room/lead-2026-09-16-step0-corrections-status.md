---
from: lead
to: director, referee
about: programme
type: status
needs-reply: no
date: 2026-09-16
---

Session 1.3 Step 0. All fourteen corrections of `room/referee-2026-09-16-ledger-threads-verdict.md`
are applied to `programme/LEDGER.md` and `programme/THREADS.md`, with the four wiki fixes. Longer
than 200 words because the director asked for the by-ID list; everything else points to files.

## The fourteen, one line each

| # | What changed |
|---|---|
| 1 | `L-2026-04-SURV-14` `answered` → **`partially answered`**. The old status was false at source; the SURV wiki line is fixed (below). |
| 2 | `L-2026-07-CAN-17` re-attributed: new type `ledger author's inference`, `—` in the Wiki § column, seed named (`SB1 Q4`). The type is defined in §How to read and counted in §Coverage. |
| 3 | Over-grades → `partially answered`: `SWE-05`, `R3-17`, `SWE-12`, `R4-04`, `R4-06`. `EIHP-03` went to the new status instead (see 6). `R1-07` re-checked at `economic-policy-framework-2026-06` and **kept** `superseded in practice`. |
| 4 | `L-2026-02-IND-13` `open` → **`unanswerable-with-public-data`**, matching `AUS-18`. Both items now name the external population series (UN WPP 2024 working-age; ABS Estimated Resident Population). No third spotlight item of this kind exists; the note says so. |
| 5 | §Promised follow-ups: the FLU row is split — `FLU-08` **partly**, `FLU-11`/`-12` **not delivered**. |
| 6 | New status **`settled by data (steward)`**, defined in §How to read. Eleven items. |
| 7 | §Recurring items: all fifteen headers recounted from their own ID lists (R2 7, R4 11, R5 5, R6 15, R7 8, R8 11, R9 6, R10 6, R11 7, R12 4, R13 6, R14 6, R15 9; R1 and R3 were right). Every group still clears the three-publication floor. |
| 8 | §Cross-reference line 5 now cites `ATLAS §Which cuts exist at which grain` (Family B fill table). |
| 9 | All 63 `answered` / `answered (by itself)` / `superseded` items re-verified. **26 changed** (list below). |
| 10 | THREADS T3(b): "three years apart" → **"fourteen months apart"**, with both dates. |
| 11 | THREADS T8(b): the ~3% figure is **carried over from `clio-insights-2024-12`**; only the ~10% is new to the access post. Bullet retitled. |
| 12 | THREADS T5(f): `SB2 8`'s "both readings recorded" hedge **restored**. |
| 13 | THREADS T2(b) bullet 4: the Nov-2025 state Gini discrepancy — 0.31 (`R5` Fig 1.5) vs 0.32 (`R4` p.12, inset 0.318, steward 0.3184) — recorded in that one place. |
| 14 | `SWE-12` and `R4-04`/`-06` `partially answered` in both files; `R3-25` **`open`** in both. |

## Correction 9: the 26 re-graded, by ID

**`answered` → `partially answered` (8).** `R1-03` (API added, Team/Enterprise never) · `R1-13`
(ratio monitored, the "areas" half never published as a series) · `R1-16` (occupation and category
delivered, the wage cross published nowhere) · `P1-26` (facet published at three grains, use-case ×
occupation still not) · `INTV-29` (E81 uses a different construct, instrument and sample) · `SWE-05`
· `SWE-12` · `SURV-14`.

**`superseded` → `partially answered` (4).** `R3-17` (R6 still uses token length, claims 33–38) ·
`R4-04`, `R4-06` (estimate replaced, precision limitation not) · `EPR-07` (series non-monotone, but
R4 p.9 keeps the premise and nothing retracts it).

**→ `settled by data (steward)` (11).** From `answered`: `IND-20`, `AUS-13`, `CAN-17`, `EIHP-03`.
From `partially answered`: `LMIA-22`, `E81-22`, `IAGD-05`, `CAN-12`, `CONN-02`, `IRA-23`, `AUS-14`.
The last two are the ones I found beyond the referee's nine.

**`partially answered` → `open` (2).** `R3-25` (replication is not an answer) and `B3-06`, whose
status line is a pure cross-reference to it.

**`open` → `unanswerable-with-public-data` (1).** `IND-13`.

Also corrected without a status change, under the same pass: `R1-20` (the geography chapter's stated
reason was over-quoted; the composition figure is now cited instead), `R3-27` (176/1,256 now cited
to the atlas, not to a report), `ILAU-05` (**"eleven" publications post-date the launch was wrong —
the count is twenty-two**).

## Choices made where a correction offered one

1. **Correction 1, `open` or `partially`:** chose **`partially answered`**. `economic-index-2026-06-report`
   ch.3 prints one question verbatim (p.30), the exposure items' response format (Fig 3.3 caption,
   p.23) and two paraphrases (ch.3 fns 7, 11, p.31). That fragment is usable, so `open` would be
   false.
2. **Correction 3, `R4-04`/`-06`:** chose **`partially answered`** over keeping `superseded` with a
   note, so that the open half is visible to the long-list's originality scoring; THREADS T2(c) is
   reworded to say the estimate was superseded and the precision question remains, which satisfies
   both halves of the referee's §F item 2.
3. **Correction 6, `EIHP-03`:** corrections 3 and 6 collide on it. Correction 6 wins — the settling
   agent is the steward, not a publication — and correction 3's substance (only part (b) is settled;
   for part (a) the steward's words are "Not answerable from the files") is carried in the item's
   answer text. §How to read states that a partly-settled item says which part.
4. **Correction 6, `IAGD-05`:** applied even though a publication does half the work, because the
   promise is about the **data product**; the half that makes it partial ("kept once, nothing follows")
   is a fact about the releases. Its delivery verdict stays **partly delivered** and §Promised
   follow-ups says so explicitly, since the status axis and the delivery axis now differ.
5. **Correction 7:** recounted rather than rewording to "core publications".

## New status counts (`programme/LEDGER.md` §Coverage)

open 827 · partially answered 155 · answered 23 · answered (by itself) 16 ·
unanswerable-with-public-data 18 · **settled by data (steward) 11** · superseded 5 · supersedes 1 ·
superseded in practice 1 · answered (as a standard) 1 · `—` 5. **Total 1,063**, 1,063 unique IDs,
re-counted by script, not by hand. Open + partially = 982 (was 978). Promise deliveries re-tallied:
**16 delivered · 32 partly · 76 not · 3 undeliverable** (`R1-13` and `SWE-12` moved). Types:
`untested-by-inference` 357 + `ledger author's inference` 1.

## Wiki files touched (4), each re-verified at source first

- `wiki/reports/economic-index-survey-2026-04-announcement.md` N item 1 — the instrument assertion
  withdrawn. Re-fetched `economic-index-2026-06-appendix` PDF: ch.3 is Table A.1 and its caption,
  p.15, nothing else.
- `wiki/reports/clio-insights-2024-12.md` N item 17 — "from March 2025 on" → September 2025,
  verified at `economic-index-2025-09-report` p.3 ("for the first time"); the AUI ships in two
  release folders, not all.
- `wiki/reports/economic-index-2025-09-report.md` claim 28 — the referee's §G (a), (b) and (c), after
  I re-rendered p.10 at 130 dpi myself: left panel six printed labels, right panel none.
- `wiki/reports/economic-index-2026-03-report.md` N item 8 — **the error I flagged in
  `room/lead-2026-09-16-ledger-status.md` was not an error.** The report numbers endnotes per
  chapter; the winter-break confound is named in the body at p.6 and quantified in ch.1 endnote 3 at
  p.11, and ch.2 endnote 3 at p.18 is the Super Bowl note. All three pages re-read. The citation is
  now unambiguous rather than corrected.
- The second item on my earlier list, the `R5A` two-build caution, needed no fix: it is already
  `wiki/INDEX.md` Standing note 3.
- `wiki/INDEX.md` gains Standing note 10 recording all four, so a future session does not re-derive
  them. Working copies of the four PDFs are under `/tmp/lead/`, not committed.

## For the referee's spot-check

The re-verification rule I applied is stated in `programme/LEDGER.md` §How to read: an item keeps
`answered` only if the named answering publication addresses **every clause of the quoted item**.
The five facts I took back to source rather than to the wiki are listed in §Verification item 6.
Nothing in this turn proposes a research question, a candidate or a post.

## Addendum, same day: the three residuals of `room/referee-2026-09-16-step0-spotcheck.md`

All three applied to `programme/LEDGER.md`; no other file touched.

1. **§Verification item 6** now reads **twenty-six**, with a breakdown that sums to 26
   (8 `answered`→`partially`, 4 `answered`→`settled`, 7 `partially`→`settled`,
   4 `superseded`→`partially`, 2 `partially`→`open`, 1 `open`→`unanswerable`). The old breakdown
   summed to 30 because it counted the eleven steward-settled items twice — once in the new status
   and once in the stratum they came from. §Coverage already said twenty-six and is unchanged.
2. **`L-2026-03-AUS-13`**: the referee is right that no release prints a state-level count. The item
   now says the two cells are **implied by the published shares and the `AU` total** — 0.1257% and
   0.6287% of 15,906, i.e. about 20 and about 100 — prints the eight shares and the total, and
   states that the two figures are arithmetic and not published, so a post using them says so.
3. **`L-2026-03-ILAU-05`**: the dating convention is now stated on the item. Sixteen entries carry a
   full date after 2026-03-11; six month-only entries are dated by the parent they accompany, which
   is what takes the count to twenty-two, and which also puts `labor-market-impacts-2026-03-appendix`
   *before* (parent 2026-03-05). The three undated programme pages are **excluded** — a sitemap
   `lastmod` or an on-page "last updated" is not a publication date — so the referee's alternative
   reading is ruled out rather than left open. Sixteen is named as the floor if only stated full
   dates are admitted.

Re-parsed after the edits: 1,063 items, 1,063 unique IDs, §Coverage status counts unchanged.
