# Referee verdict · post2 (LL-11) · brief (re-verdict after the one design pass) · 2026-09-16

Reviewed: `posts/post2/BRIEF.md` at `746cb8d` (419 lines; byte-identical in the local tree at
`7c2d793`) against the first verdict `posts/post2/notes/referee-brief.md` (`aa1c9f7`);
`posts/post2/notes/feasibility.md` at `a452d01` §1, §2, §4, §6; `room/steward-2026-09-16-recode-correction-applied.md`;
the last dated block of `room/lead-2026-09-16-brief-post2-status.md` (`a8ac4b1`);
`room/editor-2026-09-16-brief-post2.md`; `posts/post3/BRIEF.md` §1 and §4 for the citation ruling only.
Anthropic's quotations were checked against the page references established in the first verdict; no
new source was fetched. No room note between analyst and director was read; `reference/` was not opened.

## Verdict

**PASS WITH CHANGES.** Every blocking item of the first verdict is applied; no design change remains.
The redesign leaves two seams between hypotheses that one result could fall through — H1 against H3 when
both windows are negative but of different size, and H3(b) against H4 when the coding block carries the
estimate — and each is closed by one sentence the lead writes without a further referee round (changes
C1–C4 below, exact text given). Nothing here blocks Gate 1b.

## Items

**First verdict's items, one by one.**

1. **H1's shared signature (design change) — applied.** §6: H1 is "Negative correspondence", names the two
   readings (flow; anti-aligned inflows) as inseparable in six aggregate frames and quotes R5 p.6 as
   Anthropic's own gloss; H3 covers both denominators, the API side quoted from p.6; H2 is "No task-level
   correspondence", its prediction following from its premise. §5: "the first measure of the cross-surface
   correspondence the migration sentence asserts"; "leading-indicator" and "loses its anchor" are gone
   (the only remaining "leading indicator" is §7(1) recording its removal). §12 H1 ending carries the
   concession ("What it cannot say is that anything moved"). Nothing remains.
2. **Wrong occupation code — applied.** §8: 43-9111.01 Bioinformatics Technicians → 15-2099.01 carries
   9.7110 of February API classified mass; 43-9011.00 → 15-1299.00 carries 0.6081; the SOC 43 → 15 move is
   10.3190 of classified mass = 9.3168 points of the geography total, base named; "consistent with" H4
   "and does not show it"; without the task the 2019 API leg is +10.0054% (53.2231 → 58.5483), "about a
   third". Every figure matches the steward's corrected §2 and §6 and the room note. Nothing remains.
3. **Degenerate within-surface benchmark — applied.** §9 test 3 drops the statistic, states why it is −1
   by construction, sets the cross-surface expectation under no correspondence at zero and points to the
   §10 permutation null (10,000 draws) as the accounting placebo. H1's signature no longer refers to a
   benchmark. Nothing remains.
4. **§12 endings — applied.** Five endings: H1, H2 (measured null), H3, H4, windows-disagree-or-underpowered.
   The fused third paragraph is split: the H3 ending carries only H3's signature. Nothing remains; see C1
   for a case that still falls between two endings.
5. **Smallest effect of interest — applied.** §6 H2 fixes it as the correlation the panel would show if the
   whole reproduced SOC-15 movement were a flow and nothing else moved, computed from the reproduction leg
   before any cross-surface estimate is read. What remains is a pre-registration matter, not a brief
   matter: the allocation rule (how the reproduced category change is spread across the 242 coding tasks;
   non-coding changes set to zero) is not stated and must be, and the pre-registration should also print
   the fraction of the reproduced movement that corresponds to the MDE, so a null reads as "less than X of
   the coding movement could have been a flow". I will block the pre-registration without the rule.
   Not blocking here.
6. **Primary metric and effective N — applied.** §9: percentage-point change, Pearson primary, Spearman
   beside every estimate, jackknife influence beside the headline, disagreement reported as unresolved;
   coding-set MDE about 0.18 (N = 242) and complement about 0.089 (N = 999) stated. My arithmetic on the
   steward's formula: 0.1793 and 0.0885. Nothing remains.
7. **Two bounding Anthropic results — applied.** §7(3) carries the coursework fall (19% → 12%; 5 pp / 12 pp
   term-versus-break, ch. 1 endnote 3, p.11); §7(4) fifth and §9 exploratory (b) carry the API becoming less
   directive (66.30 → 63.58 → 58.22). Nothing remains.
8. **Common shock pushes positive — applied.** §7(2): classifier change Sonnet 4 → 4.5, fn 7 p.18, sign
   stated as positive and masking. Nothing remains.
9. **Vintages and bases — applied.** §3 and §7(4) attach "2010 vintage, all-conversation base" to the
   40%/34%/44%/46% and "2019 vintage, classified base" to 35% and +14%/−18%. Nothing remains.
10. **"Enterprise" / "consumer" — applied.** §7(2) carries both "mostly" hedges; the preamble and §7(2) fix
    that findings say Claude.ai and the 1P API. Nothing remains.
11. **Shared first result with post3 — applied as ruled, on post2's side.** §4 and §8: post3 states the
    vintage discrepancy as its first result; post2 reports both specifications inside its measurement
    section, cites post3, and "opens on neither". Cross-post residue for the director, not for this brief:
    `posts/post3/BRIEF.md` §4 still says post2 "headlines cross-surface task **migration**", that "the
    two share no key number", and contains no statement of the discrepancy as post3's first result. Not
    blocking for post2.
12. **Smaller points — applied.** (a) modal holder by weight, exact tie to the lower major-group number,
    tie count printed in the pre-registration; (b) "3.4 times it in August" (8.1108 / 2.4111 = 3.36);
    (c) §12 carries no numeral, "load-bearing" is gone. Nothing remains.

**What the redesign may have broken — checked.**

13. **Signatures and counter-results.** Each of H1–H4 has both. H1 (both windows negative and excluding
    zero; survives coding-block removal with renormalisation; present in non-coding) is separated from H3
    (weakens on the window contrast or on coding-block removal) and from H4 (carried by SOC-15, absent in
    the complement); H2 is the interval around zero. H1's second reading (anti-aligned inflows) is
    correctly restricted by §9 test 3 to inflows that survive (a) and (b) — inflows anti-aligned *inside*
    the non-coding panel — and the brief says those stay unobservable. Two seams remain, each one
    sentence:
    - **C1 (should; the lead applies it).** H3(a)'s signature is "weakens materially towards zero when the
      November→February window is dropped", its §12 ending is "confined to the window in which a new
      consumer population arrived", and H1's requirement is "both windows negative and excluding zero".
      The likeliest real outcome — both windows negative and excluding zero, November→February several
      times larger — satisfies H1 and H3(a) at once and has no ending. Replace, in §6 H3, "the estimate
      weakens materially towards zero when the November→February window is dropped, or when the coding
      block is removed and the remaining shares renormalised, or both" with: "the August→November
      estimate, or the coding-block-removed and renormalised estimate, or both, does not exclude zero or
      lies inside H2's smallest-effect interval, while the November→February estimate does"; and replace
      its counter-result with "negative estimates excluding zero in August→November and after coding-block
      removal with renormalisation". Add to §6 H1's signature: "the estimate reported for H1 is the
      smaller of the two windows', and any excess in November→February is reported as denominator-carried
      (H3)". This makes H1 and H3 a partition; the pre-registration may add a ratio as a descriptive
      secondary, but the decision rule is exclusion of zero.
    - **C2 (should; the lead applies it).** H3(b) and H4 share a signature: if the correspondence lives in
      the coding set, coding-block removal kills it, so H3(b) fires whenever H4 does. Add to §6 H4, after
      the signature: "H4 is the case of H3's API-side reading in which the coding set itself carries the
      correspondence: H4 holds only if the coding-set estimate is negative and excludes zero while the
      complement's does not; if the coding-block-removed estimate does not exclude zero but neither does
      the coding set's own, the outcome is H3, and the coding set's minimum detectable effect of about
      0.18 is printed beside that verdict." §12's H4 ending then fires only when its signature is met.
14. **§5 for both outcomes, free of the dropped phrases.** Yes on both. One residue, **C3 (should; §5
    freezes at Gate 1b)**: the fails-branch says the post "shows that each surface's task mix moved with no
    task-level correspondence to the other". A null shows no correspondence *detectable* at |r| ≈ 0.08 and
    smaller than the whole-movement flow of item 5; §12's H2 ending already says "within an interval that
    excludes the correspondence the reproduced coding movement would have produced". Change §5 to "with no
    task-level correspondence to the other detectable at the precision the panel affords". Twelve words;
    the claims list will hold the post to it.
15. **§12 endings.** Every hypothesis with a confirmatory test has one; the null and the mixed case have
    theirs. After C1 the mixed paragraph should also name the case C1 resolves, **C4 (could)**: add to
    §12's last paragraph's first sentence "…or the correspondence is present in both windows but of a
    size the body attributes mostly to the February denominator…" so the reader knows that case is H1 at
    the smaller magnitude and not a failure. Optional; C1 already fixes the rule.
16. **Is the null deliverable at the steward's MDE and effective N?** Yes, conditionally. At N = 1,241 the
    95% interval half-width on r is about 0.056 (1.96/√1238), so an estimate within ±0.024 of zero
    yields an interval inside ±0.08; the H2 signature requires the interval to exclude the smallest effect
    of interest, which a whole-SOC-15-movement flow (7–8 points of classified mass on each surface,
    concentrated in 242 tasks) will place well above 0.08. Condition: if the pre-registration's computed
    SEI comes out below the MDE, the null is not deliverable and the pre-registration must say so before
    any estimate is read. The coding-set null (MDE 0.18, half-width 0.13) is weak and §9 test 4 says so.
17. **§9 exploratory allowance.** Stated: two tests, after the confirmatory set, labelled exploratory,
    excluded from the headline, any further test logged as a deviation. Confirmed.
18. **The question.** Says AI; findings say Claude.ai and the 1P API. Every word names something tested:
    "task" = the 1,241-task O\*NET panel; "larger share" / "smaller share" = the sign of a percentage-point
    change in `onet_task_pct` on one surface; "AI use" = sampled records, hedged as "mostly" consumer /
    enterprise in §7(2); "enterprise API" / "consumer app" = the 1P API and Claude.ai. The "when … does
    it" form is a conditional in English and is answered by a symmetric correlation of changes; with
    "leading indicator" removed nothing in the brief reads it as a timing claim. Accepted as frozen.
19. **§8 against the steward's corrected note at column level.** Matches: count bases 964,494 / 999,875 /
    1,000,000 and 944,638 / 971,525 / 1,000,000; API `onet_task_count` rows 2,056 / 2,253 / 2,299, min 15;
    statements 19,530 × 9, 18,428 keys; crosswalk 1,164 rows, 0 unmatched, 20,081 rows out; residuals
    6.02 / 2.74 (Feb) and 8.11 / 2.41 (Aug); panel mass 80.89 / 83.22 geography total, 87.01 / 92.18 named;
    reproduction 53.8833 → 59.2074 → 61.6363 (+14.3885%), 41.9682 → 38.5001 → 34.6150 (−17.5208%), shipped
    +3.2433% / −17.4248%; 18 duplicate rows, 61.7795 if kept; 337 pairs, 34 in / 36 out, out-leg 0.3912;
    task series 1.2396 → 3.9479 → 6.7260 and 1.6426 → 1.2693 → 1.4451; coding sets 242 / 242 / 228 with
    55.29 / 46.31 API and 31.61 / 29.42 Claude.ai; directive coverage 1,143 / 1,155 / 1,135 on base
    2,055 / 2,252 / 2,298; MDEs 0.0795 / 0.0771 / 0.0701. No discrepancy found.

**Changes the lead applies, listed exactly (no further referee round):** C1 (§6 H3 signature and
counter-result; one clause in §6 H1), C2 (§6 H4, one sentence), C3 (§5, twelve words), C4 (§12 last
paragraph, optional). Nothing else in the brief changes.

## Independent re-derivations (results stage only)

Not applicable. No number was re-run in this pass; the first verdict's re-derivations (`/tmp/referee-post2-rederive.py`)
stand for every figure the brief carries, and the two figures new since then (10.0054%, 61.7795) were
produced by the steward's `data/replication/post2_recode_attribution.py` and match the first verdict's
own value (+10.01%) where the two overlap. The MDE and interval half-widths in item 16 are my arithmetic.

## Assumptions sweep

**(1) Value judgement — handled.** Share language in question and title; "exposed", "at risk",
"transformation" only in quotation; "leading indicator" and "loses its anchor" removed from §2 and §5;
§7(1) says a share moving is not a volume moving and quotes Anthropic's construct (Fig. 1.1, p.5). The
residue flagged last time is gone.

**(2) Construct mapping — handled (the design change is made).** Was "needs a design change". H1 is now
a correspondence with both readings named as inseparable; Anthropic's composition gloss on "migration"
(p.6) is quoted inside the hypothesis; the positive-sign common shock (classifier change) is stated
beside the estimate; no level compared across surfaces; the "mostly" hedges and the prompt–response-pair
unit are in the measurement paragraph. The brief's own §7(2) heading still reads "needs a design change,
and the change is specified" — a record of the pass, not an open item.

**(3) Composition or selection — handled, with one residue newly flagged (C1, C2).** Both inflows are
named as Anthropic names them (Super Bowl and casual signups, p.6 and ch. 2 endnote 3; Claude Code as a
large share of sampled API traffic, p.6), plus the coursework fall; H3 covers both denominators; the
window contrast and the renormalised coding-block cut are the tests; occupation-from-task is stated
beside the reproduced number with the Bioinformatics Technicians example. The residue is that the
widened H3 now overlaps H4 on the API side and H1 on the window contrast, which C1 and C2 close by
making the rules a partition. Neither inflow is observable in any file and the brief says so.

**(4) Anthropic's own results that cut against or bound — handled.** Five carried in §7(4): call-splitting
as H4; the Claude.ai coding decline predating the API series with vintage and base attached; the
near-closed task universe; the composition gloss on "migration"; the API becoming less directive across
the same windows, placed beside the exploratory directive test. The bounding result (API top-ten share
28% → 32% → 33%, Fig. 1.1) sits inside H2's premise. Nothing new to flag.

## What I could not verify

- Whether post3's brief will, in its own pass, state the vintage discrepancy as its first result and
  describe post2 as headlining correspondence rather than migration; today its §4 does neither. Post2's
  side of the ruling is honoured; the cross-reference is the director's to close.
- The value of the smallest effect of interest: the brief fixes how it is computed but the allocation
  rule is not yet stated, so I cannot confirm that it exceeds the MDE (item 16's condition). Checked at
  the pre-registration.
- The lead's claim that "declined nothing" is complete: I checked items 1–12 against the text and found
  each applied; I did not diff the two versions line by line beyond the sections those items name.
- Any cross-surface correlation: deliberately not computed.
